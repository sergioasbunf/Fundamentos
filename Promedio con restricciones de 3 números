#include "stdafx.h"
#include <iostream>
#include "conio.h"
 using namespace std;
 void main () {
      int a,b,c;
      float p;
      cout<<"Ingrese 3 numeros enteros menores a 10 " <<endl;
      cin>>a,b,c;
      if (a<=10 && a>=0);
          if (b<=10 && b>=0);
              if (c<=10 && c>=0){
                   p=(a+b+c)/3
                   cout<<"el promedio es "<<p<<endl;
              else
                   cout<<"error en c"<<endl;
          else 
                   cout<<"error en b"<<endl;
      else
                   cout<<"error en a"<<endl;
                   
 getch();
